_archive/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/
.eggs/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/

# Secrets — API keys live here, never commit (loaded by the renderer
# and the SDK-portability spike).
.env
.env.*
!.env.example

# Tooling
.coverage
htmlcov/

# Local instantdemo state (when running the CLI from this repo)
.instantdemo/

# Frontend build output (generated by scripts/build_gui.sh; included in
# wheel via hatch force-include).
src/instantdemo/server/web/

# Local-only test fixtures and baseline artifacts. Not committed —
# treat each subfolder as a personal scratch / backup directory.
fixtures/
# Ad-hoc research OUTPUT from explore scripts — derived from private
# apps and personal data; never commit.
scripts/explore/out/
frontend/.vite/

# Session/scratch files — never commit (see PR #65 postmortem)
.claude/plans/
BLOG_IDEAS.md
CONTENT_PLAN.md
