_archive/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/
.eggs/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/

# Tooling
.coverage
htmlcov/

# Local instantdemo state (when running the CLI from this repo)
.instantdemo/

# Frontend build output (generated by scripts/build_gui.sh; included in
# wheel via hatch force-include).
src/instantdemo/server/web/

# Local-only test fixtures and baseline artifacts. Not committed —
# treat each subfolder as a personal scratch / backup directory.
fixtures/
scripts/explore/out/tts-bakeoff/
frontend/.vite/
